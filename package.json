{
  "name": "auditready-compliance-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end Playwright tests for the AuditReady Compliance runtime",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
